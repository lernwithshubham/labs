apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-web-app
spec:
  replicas: 1                  # VIOLATION: Should be >= 3
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:latest    # VIOLATION: Should not use 'latest'